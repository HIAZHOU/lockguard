# v0.4 支持作者与试用说明

## 本次交付

- “更多”页的诊断入口下方增加低调的“支持作者”文字按钮，首页保持简洁。
- 微信和支付宝页面展示重新编码的收款码，无昵称、姓名和头像；米白与墨绿卡片装饰在二维码以外。
- 支持保存收款图片、打开对应支付应用，再由用户从扫一扫的相册入口识别。不自动发起付款、不监听付款结果。
- Android 10 及以上写入相册 Pictures/LockGuard，无需申请读取照片权限。Android 8–9 使用系统文件保存窗口，可自行选择 Pictures 目录；也可截屏保存。
- 已接入作者补充的支付宝收款码，并完成与原图收款目标的一致性核对。
- 未增加联网权限、支付 SDK 或额外后台任务。识别、提醒和省电逻辑沿用 v0.3。
- 宣传海报、发布文案和一周试用计划在 `marketing` 目录。

## 收款目标和身份显示

微信采用本轮附件第三张收款图片，支付宝采用作者随后补充的图片。最初的前两张是其他应用的设计参考，未使用其中的收款目标。原始含身份信息的图片没有加入仓库。

使用 ZXing 3.5.3 读取原码并重新编码，再解码成品核对目标逐字一致。二维码主体保留黑白高对比及完整留白，不让 AI 重画支付数据。工具源码：`LockGuard/tools/PrepareSupportQr.java`。工具所需 jar 位于忽略的 `.tools/qr`，不是应用运行依赖。

去掉图片上的文字和头像不等于匿名收款。微信/支付宝在扫码或付款确认时可能显示收款人信息，由平台控制，应用不能隐藏。首次发布前需由作者自行用支付应用扫码核对收款账户，不需要真的付款。

## 验证边界

已完成构建及 44 项回归测试，失败数为 0。Lint 为 0 错误、3 项已有警告。最终安装包中的两个二维码均与原图解码内容一致，缩至 200、240、320 像素后也能正确解码。安装包签名验证通过，与 v0.3 使用同一调试证书，可用于覆盖安装体验。

交付文件：`锁车卫士-LockGuard-v0.4.apk`；SHA-256：`2B99361D69BBEBDC0B5B7F442031527FB9423856641402BF46F1ACF03F47FAC7`。源码已与最终构建暂存目录逐文件核对一致。本版本是小范围体验用调试安装包。

当前没有通过 ADB 连接的真机，也没有运行支付应用。离线二维码一致性校验不能代替支付平台核验账户、图库可见性和真机页面布局测试。安装后请分别检查：更多 → 支持作者 → 微信/支付宝 → 保存收款码 → 相册识别；返回应用仍可正常监控；没有安装对应支付应用时有清楚提示。

本版以小范围体验为目的。保留软件名称，海报提出新的图标方向；尚未更换安装包图标。提醒方式建议参见 `marketing/发布文案与提醒体验计划.md`。
