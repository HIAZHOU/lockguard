# 共享单车「忘锁车」提醒 App — 技术方案 v1.1

> 2026-09-19 ｜ 目标：小米 15 Ultra / HyperOS 3.0.3 自用，侧载 APK，纯本地离线
> v1.1 变更：基于实测回答（小程序扫码 / 青桔优先 / 1 小时免费卡 / 校园 15 分钟 / 手机随身）重设提醒分级、检测阈值与权限清单

---

## 0. 一句话方案

安卓原生 App 在后台常驻，通过**位置两阶段判定**识别人已离开但车还在计费的状态，在约 1 分钟内分级提醒。全程不联网、不上架、不需要账号、不需要读任何其他 App 的数据。

**核心价值**：平台的提醒只在费用累积后才来，这个 App 把你离开后的 1 分钟变成提醒时点。

---

## 1. 为什么是安卓 App，不是小程序

| 约束 | 小程序 | 安卓 App |
|---|---|---|
| 后台持续运行 | ❌ 切后台被挂起 | ✅ 前台服务 |
| 后台定位权限 | ❌ 个人主体不能选交通服务类目 | ✅ 系统权限 |
| 上架 / 备案 / 资质 | 需要 | ❌ 不需要，侧载即可 |
| 精确闹钟兜底 | ❌ | ✅ AlarmManager |

**结论**：忘锁车的提醒天然是后台任务，小程序路线被权限模型封死。

---

## 2. 你的输入带来的三处设计变更

| # | 你的回答 | 造成的设计变更 |
|---|---|---|
| 1 | **小程序扫码**（不是青桔 App） | 骑行中不会有青桔 App 的常驻通知 → **通知监听层降级到 V0.2**，V0.1 纯位置判定。副作用：没有"事实确认"信号，必须靠用户点「我已锁车」，所以主界面要做得一眼能看懂状态 |
| 2 | **1 小时免费卡 + 校园骑行 ≤15 分钟** | 损失模型变了：你不会"马上被扣钱"，还有 45 分钟缓冲。真正的损失是①车被人骑走、费用记你头上 ②1 小时后开始持续扣费而你已进教学楼**完全无感**。→ **提醒分级不再以"免费时长耗尽"为急阈值**，改以"人走开多远 / 多久"为主轴 |
| 3 | **校园场景 + 手机随身** | 活动半径小 → 分离距离阈值 100 米改 **80 米**（校园里 80 米已经是"走远了"）✅ 手机随身 = 手机位置即人的位置，两阶段算法直接适用，**省掉了"手机留车筐"的基准偏移分支** |

---

## 3. 总体架构

```
LocationMonitorService（前台服务 · location 类型 · 常驻）
   └─ FusedLocationProviderClient 自适应采样 → 速度推导 → RideStateMachine
                                                              │
                                          ┌───────────────────┴────────────┐
                                          ▼                                ▼
                                   AlertDispatcher                 RideBus（StateFlow）
                                   （三级通知 + 全屏 + 动作）        └→ Compose UI
```

**关键设计：RideStateMachine 是纯 Kotlin、零 Android 依赖。** 算法能用单元测试喂时间序列验证，不用真机、不用出去骑车就能把全部边界场景跑掉。这是整个方案里最重要的一个决定。

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | Kotlin 2.0.21 | 官方主推，协程处理定位流顺畅 |
| UI | Jetpack Compose (BOM 2024.10.01) | 只需 3 个页面，代码量最小 |
| 定位 | play-services-location 21.3.0 | 系统级融合定位，比 raw GPS 省电 |
| 存储 | DataStore Preferences | 只存阈值，不需要数据库 |
| 构建 | AGP 8.7.3 / Gradle 8.9+ / JDK 17 | compileSdk 35 · targetSdk 35 · minSdk 26 |
| 单元测试 | JUnit 4 | 纯逻辑测试，无 Robolectric |

**V0.1 故意不引入 Room、不引入依赖注入、不引入通知监听。** 先把"位置判定能不能用"验证掉。

---

## 4. 核心算法：两阶段判定

### 4.1 为什么不能"远离起点就报警"

骑行途中人一直在远离起点，天真判据会**全程误报**。必须把"停车"和"离开"拆成两件事。

### 4.2 判定链条

| 阶段 | 判据 | 输出 |
|---|---|---|
| ① 骑行中 | 速度 2.8–7.0 m/s（10–25 km/h） | 骑行计时启动 |
| ② 判定已停车 | 速度 < 0.28 m/s（1 km/h）持续 ≥ 30 秒 | 锁定当前坐标为「停车点」 |
| ③ 检测步行离开 | 速度 0.8–1.7 m/s（3–6 km/h），且距**停车点**距离增大 | 进入分离候选 |
| ④ 触发提醒 | 距停车点 > 80 米 | 事件 Separated |

**两个关键点**：
- ③ 的距离基准是**停车点**，不是起点，也不是扫码点
- ① 有**速度上限 7.0 m/s（25 km/h）**——超过就判定"这不是自行车"，直接回 IDLE。没有这个上限，你开车通勤时 App 会以为你在骑车，下车走开就误报

### 4.3 回滚保护（排除等红灯误判）

等红灯会造成静止。处理方式：

- 静止后 **60 秒内**重新加速到骑行速度 → 判定为等红灯，**撤回**停车状态
- 静止超过 60 秒 → 确认真停车
- 已进入 PARKED 后又出现骑行速度 → 同样撤回
- 已进入提醒态后距离掉回阈值一半以下 → 判定"人回来了"，撤回并重置提醒等级

**整个状态机是可回滚的，不是单向链条。**

### 4.4 抗噪声处理

| 噪声源 | 处理 |
|---|---|
| 口袋/手持抖动导致速度跳变 | 速度取最近 3 个点的**中位数**（不是均值，中位数抗单点突刺） |
| 高楼/楼道 GPS 漂移 | `accuracy > 50 米` 的定位点**直接丢弃** |
| 定位点缺失（进楼、隧道） | 断点超过 60 秒宽限期 → **重启静止计时**，不用断点后的位置重算，避免锚点错位 |
| 设备不报速度（hasSpeed=false） | 用连续两点距离 / 时间差**推导速度** |

### 4.5 参数定稿（校园场景）

| 参数 | 定稿值 | 说明 |
|---|---|---|
| 骑行速度下限 | 2.8 m/s（10 km/h） | 判定"在骑车" |
| 骑行速度上限 | 7.0 m/s（25 km/h） | 超过判定为机动车，回 IDLE |
| 静止阈值 | 0.28 m/s（1 km/h） | 判定"停下了" |
| 静止确认时长 | 30 秒 | 从静止到"已停车" |
| 等红灯回滚窗口 | 60 秒 | 静止短于此且重新加速 = 等红灯 |
| 步行速度区间 | 0.8–1.7 m/s（3–6 km/h） | 判定"在走路" |
| **分离距离阈值** | **80 米** | 从停车点算（校园活动半径小） |
| 二级提醒距离 | 300 米 | 或分离满 3 分钟 |
| 三级升级时长 | 15 分钟 | 超过你典型骑行时长仍无确认 |
| 定位精度过滤 | 50 米 | 精度差于此值的点丢弃 |
| GPS 断点宽限 | 60 秒 | 超过则重启静止计时 |
| 提醒冷却 | 60 秒 | 同级提醒最小间隔 |
| 暂缓时长 | 10 分钟 | 用户点"我在推车/还在附近"后静默 |
| 免费时长 | 60 分钟 | 你的骑行卡 |
| 典型骑行时长 | 15 分钟 | 校园场景 |

**这组值必须在真实骑行中校准一遍。** 80 米 / 30 秒是按校园估计的，实测后可能要调。

---

## 5. 提醒分级（已按 1 小时卡重设）

**旧设计作废**：原方案把"距免费时长耗尽前 3 分钟"当急阈值——你免费 1 小时、只骑 15 分钟，这个阈值永远不会触发，等于设计了个死代码。

**新设计**以"人走开多远 / 多久"为主轴，免费时长只做第二道防线：

| 级别 | 触发条件 | 形式 | 文案重点 |
|---|---|---|---|
| 轻 | 距停车点 > 80 米 | 震动 ×1 + 通知 | "车可能没锁，回去看一眼" |
| 中 | 距停车点 > 300 米，或分离满 3 分钟 | 震动 ×3 + 响铃 + 通知 | "八成忘了锁车" |
| 急 | 分离满 15 分钟仍无确认 | 强提醒 + 全屏 + 响铃 | "距开始计费还有约 X 分钟" |

第三级的文案用"距开始计费还有约 X 分钟"（X = 60 − 已骑行分钟数），把 1 小时免费卡这个事实变成紧迫感来源。

### 通知里必须带的三样东西

1. **「我已锁车」** — 直接消除通知并结束本次监控（主操作）
2. **「暂缓 10 分钟」** — 用于"我在推车找车棚"这类误报场景
3. **打开青桔** — 深链接到小程序/App 的当前订单，找远程操作入口

### 一个必须承认的误报场景

你把车停在 A 点，停了 30 秒，发现位置不对，**推着车走到 100 米外的 B 点**再停。推车速度 1.0–1.5 m/s，和步行落在同一个区间，**算法无法区分**，会误报。

处理方式：不试图用算法区分，而是给「暂缓 10 分钟」按钮。这是成本最低的解法。校园里推进 80 米以上找车棚的概率不高，可接受。

---

## 6. 通知监听：V0.2 再做

**V0.1 不做**，原因是你用小程序扫码：

| 信号 | 是否可得 | 说明 |
|---|---|---|
| 青桔 App 常驻骑行通知 | ❌ | 你走小程序，App 没在跑 |
| 微信「服务通知」里的青桔推送 | ⚠️ 不确定 | 取决于你是否订阅了青桔小程序的骑行通知，覆盖率未知 |
| 微信支付扣费通知 | ⚠️ 覆盖不足 | 你有 1 小时免费卡，免费期内**不产生支付**，因此读不到 |

**V0.2 的尝试方向**：监听 `com.tencent.mm` 的通知，关键词匹配「青桔」「骑行」「已关锁」「还车」。收益不确定，但实现成本很低，值得一试。如果实测下来覆盖率低，就永久放弃这一层，纯位置判定已经够用。

---

## 7. 权限清单（小米 15 Ultra / HyperOS 3 定稿）

### 7.1 必需权限

| 权限 | 用途 | 授权方式 |
|---|---|---|
| ACCESS_FINE_LOCATION | 精确位置判定 | 弹窗允许 |
| ACCESS_COARSE_LOCATION | 粗略位置兜底 | 弹窗允许 |
| ACCESS_BACKGROUND_LOCATION | **核心**，后台定位 | **必须单独引导**，选「始终允许」 |
| FOREGROUND_SERVICE | 前台服务常驻 | 自动 |
| FOREGROUND_SERVICE_LOCATION | 声明 location 型前台服务 | manifest 声明 |
| POST_NOTIFICATIONS | 发提醒通知 | 运行时弹窗 |
| RECEIVE_BOOT_COMPLETED | 开机自启 | 自动 |
| REQUEST_IGNORE_BATTERY_OPTIMIZATIONS | 免电池优化 | 跳系统设置页 |
| USE_FULL_SCREEN_INTENT | 三级全屏提醒 | 跳系统设置页 |
| VIBRATE / WAKE_LOCK | 震动与唤醒 | 自动 |

**不声明 INTERNET 权限。** 这是物理级保证：App 在 manifest 层面就没有联网能力，符合保密要求。

### 7.2 小米 / HyperOS 专属（最容易漏、漏了就废）

| 设置项 | 路径 | 不做的后果 |
|---|---|---|
| **后台弹出界面** | 设置 → 应用设置 → 应用管理 → 本应用 → 权限管理 → 后台弹出界面 | **三级全屏提醒完全失效**。小米独有权限，不开启任何后台 Activity 都弹不出来 |
| **锁屏显示通知** | 通知管理 → 本应用 → 锁屏通知 | 手机在口袋里锁屏时，通知静默堆着，等于没提醒 |
| **横幅通知** | 通知管理 → 本应用 → 横幅通知 | 亮屏时只在通知栏堆叠，不会浮出 |
| 省电策略 | 应用管理 → 本应用 → 省电策略 → **无限制** | 被 HyperOS 限制后台定位频率 |
| 自启动 | 应用管理 → 本应用 → 自启动 | 重启后不会自动恢复监控 |
| 后台任务锁定 | 最近任务卡片 → 下拉锁定 | 划掉即被杀 |
| 定位权限 | 权限管理 → 定位 → **始终允许 + 使用精确位置** | 只选"仅使用时允许"→ 后台定位直接断 |

**「后台弹出界面」是小米系最容易被忽略的一项**，它不在标准 Android 权限体系里，Android Studio 也不会提醒你。App 内必须用图文引导用户手动打开。

### 7.3 授权引导顺序（不能一次全弹，否则用户全拒）

1. 前台定位 → 允许
2. 单独一页解释"为什么需要后台定位" → 跳系统设置选「始终允许」
3. 通知权限 → 允许
4. 电池优化白名单 → 跳系统设置
5. 全屏意图权限 → 跳系统设置
6. **小米专属清单**（7.2 那张表）→ 图文引导，逐项打勾

---

## 8. 保活策略

| 层 | 手段 | 作用 |
|---|---|---|
| 1 | location 型前台服务 + 最低优先级常驻通知 | 基础存活保障 |
| 2 | 电池优化白名单 | 免 Doze 限制 |
| 3 | HyperOS 省电策略「无限制」+ 自启动 + 任务锁定 | 防 ROM 层清理 |
| 4 | AlarmManager `setAndAllowWhileIdle` 15 分钟心跳（**非精确闹钟，不需要 SCHEDULE_EXACT_ALARM 权限**） | 万一被杀也能被唤醒并自检重启 |
| 5 | BootReceiver 监听 BOOT_COMPLETED / MY_PACKAGE_REPLACED | 重启后自动恢复 |

**不需要申请 SCHEDULE_EXACT_ALARM**——心跳用非精确闹钟足够，省掉一轮授权引导。这也是少一个权限、少一个失败点。

### 自适应采样（省电核心）

| 状态 | 采样间隔 |
|---|---|
| IDLE 待机 | 60 秒 |
| RIDING 骑行中 | 15 秒 |
| PAUSED 判定停车中 | 10 秒 |
| PARKED / 提醒中 | 10 秒 |

### 版本适配要点（须在你的机器上实测）

- **Android 13+**：不申请 POST_NOTIFICATIONS，通知会**静默消失**——症状是"App 看着在跑但从不提醒"，极易误判成保活失败，然后你去修错的地方
- **Android 14+**：前台服务必须声明 `foregroundServiceType`，且启动时机受限
- **Android 15+**：`BOOT_COMPLETED` 禁止拉起部分 FGS 类型，**但 location 类型不在禁止名单内**，所以这套方案可行

---

## 9. 边界场景清单（V0.1 必须全绿）

| # | 场景 | 期望行为 | 处理方式 |
|---|---|---|---|
| 1 | 骑行中等红灯 45 秒 | 不提醒 | 回滚窗口内重新加速 → 撤回 |
| 2 | 骑行中等长红灯 90 秒后骑走 | 不提醒 | PARKED 后出现骑行速度 → 回滚 |
| 3 | 停车后原地打电话 5 分钟 | 不提醒 | 未远离停车点 |
| 4 | 停车后推车 50 米 | 不提醒 | 低于 80 米阈值 |
| 5 | 停车后步行去 200 米外 | **提醒** | 目标场景 |
| 6 | 正常还车并确认 | 不提醒 | 用户点「我已锁车」 |
| 7 | GPS 漂移点（accuracy>50） | 不影响判定 | 直接丢弃该点 |
| 8 | 定位断 2 分钟 | 不误判锚点 | 超宽限期 → 重启静止计时 |
| 9 | 分离满 15 分钟 | 升级到三级 | escalationDurationMs |
| 10 | 开车通勤（15 m/s） | 不进入骑行态 | 速度上限 7 m/s 拦截 |
| 11 | 离开后走回停车点 | 回滚，不重复刷提醒 | 距离 < 阈值一半 → 回 PAUSED/PARKED |
| 12 | 推车找车棚（>80 米） | **会误报** | 不靠算法，靠「暂缓 10 分钟」按钮 |
| 13 | 连续移动不重复刷屏 | 每级只响一次 | 冷却 60 秒 + 等级单调递增 |
| 14 | 忘锁车且手机同时没电 | 无解 | 只能靠平台短信 |

场景 14 必须接受：**这个 App 消除的是大部分损失，不是全部损失。**

---

## 10. 测试方案

### 单元测试（主力，覆盖场景 1–4、6–13）

状态机是纯 Kotlin，直接喂合成定位序列：

```kotlin
// 骑行 → 等红灯 45 秒 → 继续骑行 → 停车 → 步行离开 200 米
fixes(0, 4.5f, 0f, 5f),      // 骑行
fixes(30_000, 0.1f, 200f, 5f),   // 红灯开始
fixes(75_000, 0.1f, 200f, 5f),   // 红灯 45 秒
fixes(90_000, 4.8f, 250f, 5f),   // 重新骑行
fixes(300_000, 0.05f, 400f, 5f), // 停车
fixes(340_000, 1.2f, 420f, 5f),  // 步行
fixes(420_000, 1.3f, 600f, 5f)   // 走远
// 断言：全程只有末尾产生一次 Separated，红灯期间零事件
```

### 真机验证（覆盖场景 5、12）

校园里做 3 次实测：正常锁车（应无提醒）、故意忘锁（应 1–2 分钟内提醒）、推车 >80 米（应误报，验证「暂缓」能救）。

---

## 11. 隐私

- **零联网**：manifest 不声明 INTERNET 权限，物理上无法上传任何数据
- **数据全本地**：位置仅在内存中参与判定，不落盘；卸载即清除
- **不采集**：通讯录、短信、相册、账号、设备标识
- **自用不上架**：不涉及备案、软著、隐私政策公示

---

## 12. 交付节奏

| 版本 | 内容 | 状态 |
|---|---|---|
| **V0.1** | 前台服务 + 自适应采样 + 状态机 + 三级提醒 + Compose UI + 小米权限引导 | **本次交付** |
| V0.2 | 尝试监听微信通知 + 历史记录 + 阈值自校准 | 待定 |
| V0.3 | 顺带加"第 12 分钟该不该换车"提醒 | 待定 |

---

## 13. 开工前还差的三条实测（你有空顺手看一眼）

| # | 要确认的事 | 怎么确认 | 影响 |
|---|---|---|---|
| 1 | 小米 15 Ultra 上「后台弹出界面」这一项的实际路径 | 进应用管理看一眼 | 决定引导图文怎么写 |
| 2 | 你在微信里骑青桔时，微信有没有推过青桔相关通知 | 骑一次，下拉通知栏看 | 决定 V0.2 值不值得做 |
| 3 | 校园里从停车点到你常去的教学楼/宿舍典型距离 | 大概估一下 | 决定 80 米要不要调成 60 米 |
