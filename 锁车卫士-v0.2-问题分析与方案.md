# v0.2 问题分析与方案

## 现象与代码原因

用户看到「已停下、26 分钟」，证明当前会话曾进入骑行态；不能据此说自动识别从未工作，也不能排除该次由手动开始。计时在旧版一直累计到手动确认锁车，所以也包含停车等待。

旧逻辑只有 GPS 回调才能推进状态。「已停下」阶段如果相邻有效定位间隔超过 60 秒，会重新开始静止确认。定位精度差于 50 米又直接丢弃。由此存在长期卡在已停下的路径，但没有手机运行日志，不能认定这就是该次现场的唯一原因。

更确定的设计缺陷是：即使进入已停车，也必须以步行速度离开至少 80 米才提醒。原地等 15 分钟、走 5–10 米进食堂/工位/宿舍不会满足条件。旧版还有待机 60 秒采样、Google 定位服务依赖、零速度被漂移位移替代、权限返回时未刷新、申请权限后服务未正确重试等问题。

## 本次落地

已在源码实现：原生 Android 定位、5 秒请求、持续速度识别、原生计步辅助、停车约 2 分钟确认提醒、定位中断约 3 分钟核实提示、独立单调时钟、简洁首页、更多页和权限返回刷新。停止把提醒写成已知未锁车，停止把估计时长当平台实际计费时间。

阈值是可调整的初始工程参数，未经过用户手机校园路测，不承诺识别率或 2 分钟绝对送达。系统杀进程与首次定位失败仍会影响结果。

## 权限即时反馈的难度

标准 Android 权限：低难度。运行时授权回调、Activity onResume、窗口重新获得焦点时重读权限即可；同时刷新通知总开关、通知通道、系统定位和电池优化状态。本版已经这样处理，无需用户退出再打开应用。[Android Activity 生命周期](https://developer.android.com/guide/components/activities/activity-lifecycle)

「及时」指从系统设置返回 App 时显示最新状态；用户停留在系统设置里，App 无法替换系统页面内容。小米自启动、后台弹出界面等私有开关缺少通用公开查询接口，无法可靠承诺自动识别每一个开关，故展示「需手动核对」。

## 5–10 米距离如何选择方案

| 方案 | 适合程度 | 成本与限制 |
|---|---|---|
| 只把 GPS 围栏缩到 5 米 | 不建议作为主方案 | 几米漂移就可能超过阈值，室内和楼旁更差 |
| GPS 识别骑行 + 计步识别下车 + 超时确认 | 当前已采用 | 适合短距离到达；有误提醒，需要暂缓和确认 |
| 加速度计 + 陀螺仪的完整活动分类 | 可作为下一阶段 | 难度中高；需采集口袋/手持/车架、骑车/走路/跑步/乘车等标注数据并跨姿态验证，不能用一个抖动阈值代替 |
| Google Activity Recognition 的骑行/步行转换 | 设备有可用 Google 服务时可辅助 | API 支持 ON_BICYCLE 等活动，但依赖 Google 服务，不作为国行设备唯一检测路径 |
| 平台明确的骑行中 / 还车成功信号 | 如果能取得，是判断订单结束的更直接证据 | 需核实实际平台是否发通知或提供接口；微信小程序不保证有稳定可读通知，本次未实现监听，也未读取其他 App 数据 |
| 车端蓝牙/锁状态接口 | 若平台开放可更直接 | 普通第三方 App 未必有权限或接口；本项目尚无接入，不能承诺通用可行 |

手机 GPS 在开阔环境也有数米量级误差，楼宇、桥梁、树木附近更差。因此把 5–10 米当精确的人车分离边界缺乏稳定性。[GPS.gov 精度说明](https://www.gps.gov/gps-accuracy)

Android 原生提供计步传感器，Android 10+ 需要身体活动权限。本版仅用计步辅助推断行为变化，没有训练完整 IMU 分类器。[Android 运动传感器](https://developer.android.com/develop/sensors-and-location/sensors/sensors_motion)

Google 活动转换 API 能报告 ON_BICYCLE、WALKING 等类别，但仍是活动推断，不是共享单车锁或订单状态。[ActivityRecognitionClient](https://developers.google.com/android/reference/com/google/android/gms/location/ActivityRecognitionClient)

## 验证范围

以实际 Kotlin 状态机测试为准，覆盖低速校园骑行、尖峰、步行、汽车速度、稀疏采样、原地停留、5/10 米、进楼断定位、短红灯、直接转步行、计步辅助、定位漂移、确认、暂缓和等级升级。APK 编译与 Android Lint 另行验证。

仍需要在用户的小米手机验证：息屏通知、定位首点延迟、计步可用性、厂商后台策略、一天耗电，以及真实骑行识别率。当前没有连接真机，不能把合成测试结果表述为已经解决全部现场问题。
