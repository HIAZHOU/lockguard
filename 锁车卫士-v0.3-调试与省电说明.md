# v0.3 真机调试、耗电验证与备份

## 这次日志说明了什么

截图中的 10:19:24、10:22:25 已分别触发 1 级、2 级停车提醒。10:30:49 出现 `network · 76 km/h`，紧接着记录「速度超出自行车范围」并回到待机。这是程序错误地把异常定位当作结束会话条件，不能据此认为车锁已经关闭。

旧版把不同来源的位置保存到同一个前一定位点中，且允许网络点参与速度判断；无法仅凭截图断定这次 76 km/h 是直接上报还是两点推算，但两个路径都不应有权清理未确认会话。v0.3 已禁用网络点的速度判定，并在状态机内再次拒绝超范围速度；恢复骑行也需要连续证据。

两张耗电截图间隔约 16 分钟，GPS 统计从 19:53 增至 35:23，增加 15:30，说明定位基本持续运行。截图百分比不能直接当作整块电池消耗的百分点，需与同一统计区间的实际电量、充电状态及 GPS 时间一起看。

## v0.3 的参数与取舍

- 正常通道：约 8 km/h（2.2 m/s）以上，连续至少 8 秒。
- 慢骑通道：约 6.1 km/h（1.7 m/s）以上，连续至少 30 秒，且没有明显连续步频。
- 启动要求定位精度优于或等于 25 米；正常定位请求间隔 3 秒。确认时长从合格定位开始计算，不含首次 GPS 定位的等待。
- 1.3–1.7 m/s 的中间区间不反复切换骑行/停车；有步频时仍可识别下车。
- 不保证能区分所有快走、慢跑、低速机动车和骑行。未授权身体活动或设备没有计步器时，误判风险更高。参数是下一轮路测起点，不是测得的最优值。
- 减速/步行后约 2 分钟确认提醒继续保留；走到工位但未锁车，不会被一次异常高速点清除。

## 待机省电策略

| 状态 | GPS / 唤醒策略 |
|---|---|
| 初次启动 | 最多约 60 秒定位检测 |
| 确认已锁车 | 关闭持续 GPS，进入运动唤醒守候；短时冷却防止刚离开车时重复识别 |
| 静止待机且有可用显著运动唤醒传感器 | 不周期性请求 GPS，保持低功耗传感器与被动定位 |
| 检测到运动 | 开启约 60 秒定位窗口，反复运动不能无限续期；两次运动窗口至少间隔 90 秒起点 |
| 无运动唤醒传感器 | 每约 3 分钟尝试 45 秒窗口；系统深度休眠可延后 |
| 骑行 / 刚减速 | 连续定位，以便尽快识别状态变化 |
| 已进入等待锁车确认 | 停止持续 GPS，运动唤醒和间歇窗口检查是否重新骑走 |
| 定位中断已发核实提示 | 保留骑行会话，定位降为间歇检测 |
| 首次停车提醒已发出 | 释放持续 CPU 唤醒锁，后续升级通过定时器及非精确唤醒闹钟兜底 |

Android 12+ 的短窗口还设置了定位请求自身的有效期，避免只依赖应用回调关闭 GPS。旧系统用有超时的唤醒锁保护短窗口关闭。

「更多」显示本次进程的 GPS 请求时长，日志记录 GPS 开启/关闭。此值是应用请求时长，不等同于系统统计的真实硬件耗电。

需要接受的限制：省电与唤醒速度存在取舍；非精确闹钟在 Doze 中可延迟，后续 2/3 级提醒不能保证准点。用户强行停止、厂商杀进程、重启仍可能丢失内存会话，本次没有实现完整会话持久化。不要依赖本工具作为平台还车成功的证明。

## 多品牌设置

更多页统一显示「运行保障 → 一键检查与设置」，先处理缺失的基础权限，再进入系统设置。权限详情和日志默认收起，只显示当前手机的本机建议。

品牌识别覆盖 OPPO、vivo、Xiaomi、realme、iQOO、Redmi、HONOR、nubia，并对部分子品牌及未知品牌提供回退。识别依据为系统报告的品牌与制造商；ROM 名称和菜单是提示，不代表已逐机适配或能验证厂商私有开关。

使用标准 Android 设置入口，不硬编码容易失效的厂商私有 Activity。所谓一键是统一检查/跳转入口，权限与后台设置仍由用户在系统页面确认。不能静默给所有品牌授权。

## USB 真机调试（推荐先做）

1. 手机设置中启用开发者选项和 USB 调试，使用可传输数据的 USB 线连接电脑。
2. 手机弹窗中允许这台电脑调试。
3. 在 PowerShell 执行：

```powershell
& 'D:\AndroidSDK\platform-tools\adb.exe' devices
```

设备列表应为 `device`；`unauthorized` 表示尚未在手机确认。空列表需要检查数据线、驱动和 USB 模式。

安装本次 APK：

```powershell
& 'D:\AndroidSDK\platform-tools\adb.exe' install -r 'D:\锁车卫士\锁车卫士-LockGuard-v0.3.apk'
```

然后手动打开应用、完成设置。读取日志和收集诊断：

```powershell
& 'D:\锁车卫士\LockGuard\tools\debug_device.ps1' -Live
```

脚本仅采集本应用日志、应用权限、服务和电池诊断，不清空日志，不重置耗电统计，不改变系统省电策略。输出位于根目录 `diagnostics`，已被 Git 忽略。实时日志按 Ctrl+C 结束；多台设备用 `-Serial` 指定。

也可以用 Android Studio 打开 LockGuard，选择手机后点击 Debug，在 `RideStateMachine.onLocation`、`RideMonitorService.handleLocation` 设置断点。断点会干扰实际计时，路测和耗电测试时应使用日志而不是暂停进程。

## 无线调试与离线日志

Android 11+ 可在开发者选项开启无线调试，与电脑连接同一网络。在手机选择「使用配对码配对设备」，依次执行：

```powershell
& 'D:\AndroidSDK\platform-tools\adb.exe' pair '手机IP:配对端口'
& 'D:\AndroidSDK\platform-tools\adb.exe' connect '手机IP:调试端口'
```

输入手机显示的配对码。配对端口与调试端口通常不同。无线调试自身会影响耗电，正式耗电对比时关闭无线调试、不接充电线，完成后再接线采集。

不连接电脑也可以在「更多 → 分享诊断」导出本次进程的文本日志。不含坐标；进程被杀后内存日志会丢失，所以发现问题后及时导出。

## 建议实测顺序

1. 普通走路 5 分钟：检查不进入骑行；授权身体活动再测试一次快走。
2. 室外正常慢骑：记录实际开始骑行时刻与应用识别时刻；楼旁和开阔处各测一趟，不需要拼命加速。
3. 停车后走 5–10 米进楼：应有首次提醒；不确认，等待并走 50 米，确认会话不被网络点结束。
4. 确认已锁车后静放 30 分钟：日志应出现 GPS 关闭；有运动唤醒传感器时，GPS 请求时间不应按墙钟持续增加。
5. 再拿起手机骑行：确认能重新唤醒，记录从移动到识别的延迟。锁车后 90 秒内处于冷却时，自动识别可能稍晚，可手动开始。
6. 每种品牌至少测试定位首次可用、身体活动权限、返回设置刷新、息屏 30 分钟、运动唤醒、通知送达。

当前开发电脑没有连接手机；不能将自动化测试与编译成功表述为真实耗电下降了多少，或所有品牌已实测通过。

## Git 备份

已建立项目根目录本地仓库，v0.2 修改前有 `v0.2-baseline` 标签；v0.3 完成后有对应提交和标签。构建缓存、SDK 本机路径、APK、调试记录和密钥不入库。

```powershell
git log --oneline --decorate
git diff v0.2-baseline..v0.3 -- LockGuard/app/src
```

`backups/lockguard-v0.3.bundle` 包含本地提交与标签，可在其他目录恢复：

```powershell
git clone 'D:\锁车卫士\backups\lockguard-v0.3.bundle' 'D:\LockGuardRecovered'
```

这是本地备份，没有向 GitHub 等远端上传；同盘文件仍不防磁盘损坏，可将 bundle 另存到其他磁盘。首次构建时需要重新配置 `LockGuard/local.properties` 的 SDK 路径。

## 官方依据

- [Android 显著运动与计步传感器](https://developer.android.com/develop/sensors-and-location/sensors/sensors_motion)：显著运动传感器是一次触发，需重新注册。
- [Android 定位耗电优化](https://developer.android.com/develop/sensors-and-location/location/battery/optimize)：持续高频定位有电量代价。
- [Android 真机调试](https://developer.android.com/studio/run/device) 与 [ADB](https://developer.android.com/tools/adb)：USB / 无线连接方法。
- [HONOR 应用启动管理](https://www.honor.com/uk/support/content/en-us00406923/) 与 [nubia 电池管理说明](https://www.nubia.com/en/support/faqs/troubleshooting/device/what-is-function-of-intelligent-battery-management-.html)：厂商的后台策略不同，不能用一个私有设置入口覆盖所有 ROM。
